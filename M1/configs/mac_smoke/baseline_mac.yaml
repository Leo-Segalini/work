model:
  id: "Qwen/Qwen3-0.6B"
  revision: "c1899de289a04d12100db370d81485cdf75e47ca"
  dtype: "float16"

generation:
  max_new_tokens: 256
  do_sample: false
  temperature: 0.0

prompt:
  enable_thinking: false

experiment:
  hypothesis: "Smoke Mac uniquement — vérifier que le chargement et une passe d'évaluation partielle fonctionnent sur MPS/CPU."
  changed_variable: "aucune (smoke, pas une variation expérimentale)"
  expected_effect: "Pipeline exécutable localement ; métriques non comparables au GPU commun."
  smoke: true
  note: "Ne pas utiliser pour la promotion ni comme run de référence."
